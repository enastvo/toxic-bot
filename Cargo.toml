[package]
name = "signal-bot"
version = "0.1.0"
edition = "2021"
authors = ["enastvo"]
license = "GPL-3.0-or-later"
description = "A self-hosted Signal chat bot backed by a local Ollama LLM, with per-room personalities and a LAN web dashboard."
repository = "https://github.com/enastvo/toxic-bot"

[dependencies]
tokio = { version = "1", features = ["full"] }
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio-rustls", "sqlite", "macros", "migrate"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
axum = { version = "0.7", features = ["macros"] }
axum-server = { version = "0.7", features = ["tls-rustls"] }
rustls = "0.23"
tower = "0.5"
tower-http = { version = "0.6", features = ["trace", "fs"] }
tower-sessions = "0.13"
askama = "0.12"
argon2 = "0.5"
notify = "6"
clap = { version = "4", features = ["derive", "env"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
async-trait = "0.1"
thiserror = "2"
anyhow = "1"
rcgen = "0.13"
futures = "0.3"
time = { version = "0.3", features = ["formatting"] }
chrono = "0.4"

[dev-dependencies]
tempfile = "3"
